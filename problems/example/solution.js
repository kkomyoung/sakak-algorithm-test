/**
 * 두 수의 합
 * 배열에서 두 수를 더해 target이 되는 인덱스 쌍을 반환
 *
 * @param {number[]} nums - 숫자 배열
 * @param {number} target - 목표 합
 * @returns {number[]} - 두 인덱스 [i, j]
 */
function twoSum(nums, target) {
  // TODO: 테스트를 먼저 작성한 후 구현
}

module.exports = { twoSum };